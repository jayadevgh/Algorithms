#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
	//Console
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
  	int row, col;
  	cin >> row >> col;
  	vector<string> grid;
  	for(int i =0;i < row; i++) cin >> grid[i];

  	vector<int> order(row, 1);
  	for(int j = 0;j = col; j++){
  		vector<int> neworder(row);
  		int idx = 1;
  		int newidx =1;
  		for(int i =0;i < row; i++){
  			
  		}
  	}
}