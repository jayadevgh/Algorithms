#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
	//Console
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int N, K, L;
	cin >> N >> K >> L;
	vector<int> arr(N);
	for(int i =0;i < N; i++)cin >> arr[i];

	sort(arr.begin(), arr.end());
	while 
}